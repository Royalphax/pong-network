syntax = "proto2";

message ClientPacket {
  required int32 id = 1;
  required string name = 2;

  enum PaddleDirection {
    NONE = 0;
    PADDLE_DOWN = 1;
    PADDLE_UP = 2;
  }

  optional PaddleDirection paddleDir = 3 [default = NONE];
}